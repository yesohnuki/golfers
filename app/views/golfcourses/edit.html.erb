<div class="container">
  <div class="row no-gutters">
    <div class="col-md-5 col-sm-10 mx-2 mx-sm-auto shadow-lg rounded px-0 my-5">
      <h5 class="text-dark bg-light p-4 mb-0">編集画面</h5>
      <div class="form-body p-4">
      <%= form_with model: @golfcourse do |f| %>
        <h4>ゴルフ場</h4>
        <%= f.text_field :title %>
        <h4>説明</h4>
        <%= f.text_area :body %>
        <%= f.submit '保存' %>
      <% end %>
      </div>
    </div>
  </div>
</div>
